import { MyfocusDirective } from './myfocus.directive';

describe('MyfocusDirective', () => {
  it('should create an instance', () => {
    const directive = new MyfocusDirective();
    expect(directive).toBeTruthy();
  });
});
